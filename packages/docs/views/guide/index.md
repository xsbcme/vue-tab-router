# VueTabRouter 是什么？
VueTabRouter 是一个基于 vue 的标签页路由插件，可以快速实现标签页路由。
功能包括：
- 动态路由选择
- 精细化的路由功能
- 基于组件/文件的路由配置
- 个性化组件路由传参
- 自定义组件路由过渡效果
- ...

## 对比 VueRouter
VueTabRouter拥有独立设计的API，只依赖于Vue.js，且侧重点不同，它作用在某一个页面以实现多文档处理的工作模式，比VueRouter作用范围更小。

> [!TIP]
> VueTabRouter 与 VueRouter 两者可结合使用，实现更丰富的功能。