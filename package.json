{
  "name": "vue-tab-router",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "turbo run build",
    "build:plugin": "turbo run build --filter=./packages/vue-tab-router",
    "dev": "turbo run build --filter=!./apps/* && turbo run dev",
    "publish": "pnpm run build && pnpm changeset && pnpm changeset version && pnpm changeset publish",
    "publish:beta": "pnpm run build:plugin && pnpm changeset pre enter beta && pnpm changeset && pnpm changeset version && pnpm changeset pre exit && pnpm changeset publish"
  },
  "author": "xsbcme",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/node": "^20.11.24",
    "@vitejs/plugin-vue": "5.0.4",
    "vite": "5.2.8",
    "turbo": "^1.12.4",
    "typescript": "^5.3.3",
    "vue-tsc": "^2.0.3",
    "vite-plugin-dts": "^3.1.1",
    "sass": "1.59.3"
  },
  "dependencies": {
    "vue": "3.4.21"
  }
}